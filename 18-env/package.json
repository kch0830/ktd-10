{
  "scripts": {
    "start": "cross-evn NODE_ENV=development node index.js",
    "start:prod": "cross-env NODE_ENV=development node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  }
}

